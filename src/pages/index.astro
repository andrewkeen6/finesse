---
import Layout from '../layouts/Layout.astro';
import Hero from '../components/Hero.astro';
import Stats from '../components/Stats.astro';
import BrandLogos from '../components/BrandLogos.astro';
import GoogleReviews from '../components/GoogleReviews.tsx';
import { contact } from '../data/siteConfig';
---

<Layout title="Finèsse Detailing - Fine Motorcar & Marine Detailing Services" description="Fine motorcar & marine detailing services in Port Washington, NY. Exterior, interior, ceramic coating, and paint protection. Professional results with 5-star reviews.">
  <Hero />

  <!-- Stats Section -->
  <Stats />

  <!-- Brand Logos -->
  <BrandLogos />

  <!-- Why Choose Us Section -->
  <section class="why-choose section">
    <div class="container">
      <h2 class="section-title fade-in">Why Choose Finèsse Detailing</h2>
      <div class="features-grid">
        <div class="feature-card fade-in fade-in-delay-1">
          <div class="feature-icon">
            <svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M14.7 6.3a1 1 0 0 0 0 1.4l1.6 1.6a1 1 0 0 0 1.4 0l3.77-3.77a6 6 0 0 1-7.94 7.94l-6.91 6.91a2.12 2.12 0 0 1-3-3l6.91-6.91a6 6 0 0 1 7.94-7.94l-3.76 3.76z"/></svg>
          </div>
          <h3>Professional Equipment</h3>
          <p>We use only professional-grade products and cutting-edge equipment to ensure the best results for your vehicle.</p>
        </div>
        <div class="feature-card fade-in fade-in-delay-2">
          <div class="feature-icon">
            <svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><circle cx="12" cy="12" r="6"/><circle cx="12" cy="12" r="2"/></svg>
          </div>
          <h3>Attention to Detail</h3>
          <p>Every inch of your vehicle receives meticulous care. We don't cut corners - we perfect them.</p>
        </div>
        <div class="feature-card fade-in fade-in-delay-3">
          <div class="feature-icon">
            <svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/></svg>
          </div>
          <h3>Paint Protection</h3>
          <p>Long-lasting ceramic coatings and paint protection that keeps your vehicle looking new for years.</p>
        </div>
        <div class="feature-card fade-in fade-in-delay-4">
          <div class="feature-icon">
            <svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M2 21c.6.5 1.2 1 2.5 1 2.5 0 2.5-2 5-2 1.3 0 1.9.5 2.5 1 .6.5 1.2 1 2.5 1 2.5 0 2.5-2 5-2 1.3 0 1.9.5 2.5 1"/><path d="M19.38 20A11.6 11.6 0 0 0 21 14l-9-4-9 4c0 2.9.94 5.34 2.81 7.76"/><path d="M19 13V7a2 2 0 0 0-2-2H7a2 2 0 0 0-2 2v6"/><path d="M12 10v4"/><path d="M12 2v3"/></svg>
          </div>
          <h3>Marine Specialists</h3>
          <p>Not just cars - we're experts in boat detailing too. Your marine vessel deserves the same finesse.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- Reviews Section -->
  <GoogleReviews client:load />

  <!-- CTA Section -->
  <section class="cta-section">
    <div class="container">
      <div class="cta-content fade-in">
        <h2>Ready to Transform Your Vehicle?</h2>
        <p>Experience the difference that professional detailing makes. Book your appointment today.</p>
        <div class="cta-buttons">
          <a href="/contact" class="btn btn-outline large">Get a Quote</a>
        </div>
      </div>
    </div>
  </section>
</Layout>

<style>
  /* Why Choose Us */
  .why-choose {
    background: transparent;
    padding: 5rem 0;
    border-top: 1px solid var(--dark-gray);
    position: relative;
  }

  .why-choose::before {
    content: '';
    position: absolute;
    top: 0;
    left: 50%;
    transform: translateX(-50%);
    width: 100px;
    height: 3px;
    background: var(--primary-red);
  }

  .features-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 2rem;
    max-width: 1000px;
    margin: 0 auto;
  }

  .feature-card {
    background: var(--card-dark);
    padding: 2rem;
    border-radius: 15px;
    text-align: center;
    border: 1px solid rgba(140, 43, 43, 0.3);
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.3), 0 0 0 1px rgba(140, 43, 43, 0.1);
    transition: transform 0.3s cubic-bezier(0.4, 0, 0.2, 1), box-shadow 0.3s ease, border-color 0.3s ease;
  }

  .feature-card:hover {
    transform: translateY(-5px);
    box-shadow: 0 20px 40px rgba(0, 0, 0, 0.4), 0 0 0 1px rgba(140, 43, 43, 0.3);
    border-color: rgba(140, 43, 43, 0.5);
  }

  .feature-card:hover .feature-icon {
    transform: scale(1.1);
  }

  .feature-icon {
    margin-bottom: 1.5rem;
    color: var(--primary-red);
    transition: transform 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .feature-icon svg {
    width: 48px;
    height: 48px;
  }

  .feature-card h3 {
    color: var(--white);
    font-size: 1.35rem;
    margin-bottom: 1rem;
    font-weight: 700;
    letter-spacing: -0.01em;
  }

  .feature-card p {
    color: rgba(255, 255, 255, 0.75);
    line-height: 1.7;
    font-size: 0.95rem;
  }

  /* CTA Section */
  .cta-section {
    background: linear-gradient(135deg, var(--primary-red), var(--dark-red), #4a1515, var(--primary-red));
    background-size: 300% 300%;
    animation: gradientShift 8s ease infinite;
    padding: 6rem 0;
    text-align: center;
    margin-top: 3rem;
    border-top: 1px solid var(--dark-gray);
    position: relative;
    overflow: hidden;
  }

  .cta-section::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: radial-gradient(ellipse at center, transparent 0%, rgba(0, 0, 0, 0.2) 100%);
    pointer-events: none;
  }

  @keyframes gradientShift {
    0% { background-position: 0% 50%; }
    50% { background-position: 100% 50%; }
    100% { background-position: 0% 50%; }
  }

  .cta-content h2 {
    font-size: 2.75rem;
    color: var(--white);
    margin-bottom: 1rem;
    font-weight: 800;
    letter-spacing: -0.02em;
    text-shadow: 0 2px 20px rgba(0, 0, 0, 0.3);
  }

  .cta-content p {
    font-size: 1.25rem;
    color: rgba(255, 255, 255, 0.95);
    margin-bottom: 2rem;
    max-width: 650px;
    margin-left: auto;
    margin-right: auto;
    line-height: 1.6;
  }

  .cta-buttons {
    display: flex;
    gap: 1rem;
    justify-content: center;
    flex-wrap: wrap;
  }

  .btn.large {
    padding: 1.2rem 2.5rem;
    font-size: 1.1rem;
  }

  .cta-section .btn-outline {
    background: transparent;
    color: var(--white);
    border-color: var(--white);
  }

  .cta-section .btn-outline:hover {
    background: var(--white);
    color: var(--primary-red);
  }

  /* Mobile Responsive */
  @media (max-width: 768px) {
    .features-grid {
      grid-template-columns: 1fr;
      gap: 1.5rem;
    }

    .feature-card {
      padding: 1.5rem;
    }

    .cta-content h2 {
      font-size: 2rem;
    }

    .cta-content p {
      font-size: 1rem;
    }

    .cta-buttons {
      flex-direction: column;
      align-items: center;
    }

    .btn.large {
      width: 100%;
      max-width: 300px;
    }
  }

  @media (min-width: 769px) and (max-width: 900px) {
    .features-grid {
      max-width: 800px;
    }
  }
</style>
