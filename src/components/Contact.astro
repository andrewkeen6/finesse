---
---

<section class="contact section">
  <div class="container">
    <div class="contact-header">
      <h2 class="section-title">Get In Touch</h2>
      <p class="section-subtitle">
        Ready to give your car the premium treatment it deserves? Contact us today!
      </p>
    </div>

    <div class="contact-content">
      <div class="contact-info">
        <div class="info-card">
          <h3>Contact Information</h3>
          <div class="contact-details">
            <div class="detail-item">
              <div class="detail-icon">📞</div>
              <div class="detail-content">
                <h4>Phone</h4>
                <p><a href="tel:+15167673637">(516) 767-3637</a></p>
              </div>
            </div>

            <div class="detail-item">
              <div class="detail-icon">📧</div>
              <div class="detail-content">
                <h4>Email</h4>
                <p><a href="mailto:info@finessedetailing.com">info@finessedetailing.com</a></p>
              </div>
            </div>

            <div class="detail-item">
              <div class="detail-icon">📍</div>
              <div class="detail-content">
                <h4>Address</h4>
                <p>439B Main Street<br>Port Washington, NY 11050</p>
              </div>
            </div>

            <div class="detail-item">
              <div class="detail-icon">🕒</div>
              <div class="detail-content">
                <h4>Hours</h4>
                <div class="hours-list">
                  <p><strong>Monday - Friday:</strong> 8:00 AM - 6:00 PM</p>
                  <p><strong>Saturday:</strong> 9:00 AM - 5:00 PM</p>
                  <p><strong>Sunday:</strong> By Appointment</p>
                </div>
              </div>
            </div>
          </div>

          <div class="service-area">
            <h4>🚗 Mobile Service Available</h4>
            <p>We serve a 20-mile radius from our location. Mobile service fee may apply.</p>
          </div>
        </div>

        <div class="quick-actions">
          <a href="/book" class="btn btn-primary large">Book Service Online</a>
          <a href="tel:+15167673637" class="btn btn-outline large">Call or Text</a>
        </div>
      </div>

      <div class="contact-form-section">
        <div class="form-card">
          <h3>Send Us a Message</h3>
          <form class="contact-form" id="contact-form">
            <div class="form-row">
              <div class="form-group">
                <label for="name">Full Name *</label>
                <input type="text" id="name" name="name" required>
              </div>
              <div class="form-group">
                <label for="phone">Phone Number *</label>
                <input type="tel" id="phone" name="phone" required>
              </div>
            </div>

            <div class="form-group">
              <label for="email">Email Address *</label>
              <input type="email" id="email" name="email" required>
            </div>

            <div class="form-group">
              <label for="service">Service Interested In</label>
              <select id="service" name="service">
                <option value="">Select a service...</option>
                <option value="complete">Complete Detail</option>
                <option value="exterior">Exterior Detail</option>
                <option value="interior">Interior Detail</option>
                <option value="ceramic">Ceramic Coating</option>
                <option value="other">Other / Consultation</option>
              </select>
            </div>

            <div class="form-group">
              <label for="vehicle">Vehicle Information</label>
              <input type="text" id="vehicle" name="vehicle" placeholder="Year, Make, Model (e.g., 2022 BMW X5)">
            </div>

            <div class="form-group">
              <label for="message">Message</label>
              <textarea id="message" name="message" rows="5" placeholder="Tell us about your vehicle's condition, specific concerns, or any questions you have..."></textarea>
            </div>

            <div class="form-options">
              <label class="checkbox-label">
                <input type="checkbox" name="mobile_service" id="mobile_service">
                <span class="checkmark"></span>
                I'm interested in mobile service
              </label>
              <label class="checkbox-label">
                <input type="checkbox" name="newsletter" id="newsletter">
                <span class="checkmark"></span>
                Subscribe to our newsletter for tips and special offers
              </label>
            </div>

            <button type="submit" class="btn btn-primary large">Send Message</button>
          </form>
        </div>
      </div>
    </div>

    <div class="map-section">
      <div class="map-header">
        <h3>Visit Our Location</h3>
        <p>Located in the heart of the automotive district with easy parking</p>
      </div>

      <div class="map-container">
        <div class="map-placeholder" id="google-map">
          <div class="map-overlay">
            <div class="location-marker">📍</div>
            <div class="location-info">
              <h4>Finésse Detailing</h4>
              <p>439B Main Street<br>Port Washington, NY 11050</p>
              <div class="map-actions">
                <button class="map-btn" onclick="openDirections()">Get Directions</button>
                <button class="map-btn" onclick="viewLargerMap()">View Larger Map</button>
              </div>
            </div>
          </div>
          <!-- Google Maps embed will be loaded here -->
          <iframe
            src="https://www.google.com/maps/embed?pb=!1m18!1m12!1m3!1d3024.123456789!2d-74.006!3d40.7128!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0x0%3A0x0!2zNDDCsDQyJzQ2LjEiTiA3NMKwMDAnMjEuNiJX!5e0!3m2!1sen!2sus!4v1234567890"
            width="100%"
            height="400"
            style="border:0;"
            allowfullscreen=""
            loading="lazy"
            referrerpolicy="no-referrer-when-downgrade">
          </iframe>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .contact {
    background: var(--white);
  }

  .contact-header {
    text-align: center;
    margin-bottom: 4rem;
  }

  .contact-content {
    display: grid;
    grid-template-columns: 1fr 1.5fr;
    gap: 4rem;
    margin-bottom: 4rem;
  }

  .info-card,
  .form-card {
    background: var(--light-gray);
    padding: 2.5rem;
    border-radius: 15px;
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
  }

  .info-card h3,
  .form-card h3 {
    color: var(--black);
    font-size: 1.5rem;
    font-weight: 600;
    margin-bottom: 2rem;
  }

  .contact-details {
    margin-bottom: 2rem;
  }

  .detail-item {
    display: flex;
    align-items: flex-start;
    gap: 1rem;
    margin-bottom: 1.5rem;
  }

  .detail-icon {
    font-size: 1.5rem;
    margin-top: 0.2rem;
  }

  .detail-content h4 {
    color: var(--black);
    font-weight: 600;
    margin-bottom: 0.25rem;
    font-size: 1rem;
  }

  .detail-content p,
  .detail-content a {
    color: var(--accent-gray);
    text-decoration: none;
    line-height: 1.4;
  }

  .detail-content a:hover {
    color: var(--primary-red);
  }

  .hours-list p {
    margin-bottom: 0.25rem;
    font-size: 0.9rem;
  }

  .service-area {
    background: var(--white);
    padding: 1.5rem;
    border-radius: 10px;
    border-left: 4px solid var(--primary-red);
  }

  .service-area h4 {
    color: var(--black);
    margin-bottom: 0.5rem;
    font-size: 1rem;
  }

  .service-area p {
    color: var(--accent-gray);
    font-size: 0.9rem;
  }

  .quick-actions {
    display: flex;
    flex-direction: column;
    gap: 1rem;
    margin-top: 2rem;
  }

  .btn.large {
    padding: 1.2rem 2rem;
    font-size: 1.1rem;
    text-align: center;
  }

  /* Form Styles */
  .contact-form {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
  }

  .form-row {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1rem;
  }

  .form-group {
    display: flex;
    flex-direction: column;
  }

  .form-group label {
    color: var(--black);
    font-weight: 500;
    margin-bottom: 0.5rem;
  }

  .form-group input,
  .form-group select,
  .form-group textarea {
    padding: 1rem;
    border: 2px solid #E5E5E5;
    border-radius: 8px;
    font-size: 1rem;
    transition: border-color 0.3s ease;
    background: var(--white);
    color: var(--black);
  }

  .form-group input:focus,
  .form-group select:focus,
  .form-group textarea:focus {
    outline: none;
    border-color: var(--primary-red);
  }

  .form-group textarea {
    resize: vertical;
    min-height: 120px;
  }

  .form-options {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .checkbox-label {
    display: flex;
    align-items: center;
    gap: 0.8rem;
    cursor: pointer;
    font-size: 0.9rem;
    color: var(--accent-gray);
  }

  .checkbox-label input[type="checkbox"] {
    width: 18px;
    height: 18px;
    accent-color: var(--primary-red);
  }

  /* Map Styles */
  .map-section {
    background: var(--light-gray);
    padding: 3rem 2rem;
    border-radius: 15px;
  }

  .map-header {
    text-align: center;
    margin-bottom: 2rem;
  }

  .map-header h3 {
    color: var(--black);
    font-size: 1.5rem;
    font-weight: 600;
    margin-bottom: 0.5rem;
  }

  .map-header p {
    color: var(--accent-gray);
  }

  .map-container {
    position: relative;
    border-radius: 15px;
    overflow: hidden;
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
  }

  .map-placeholder {
    position: relative;
    height: 400px;
    background: var(--dark-gray);
  }

  .map-overlay {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: rgba(26, 26, 26, 0.7);
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    color: var(--white);
    z-index: 2;
    opacity: 1;
    transition: opacity 0.3s ease;
  }

  .map-placeholder:hover .map-overlay {
    opacity: 0;
  }

  .location-marker {
    font-size: 3rem;
    margin-bottom: 1rem;
    color: var(--primary-red);
  }

  .location-info {
    text-align: center;
  }

  .location-info h4 {
    font-size: 1.3rem;
    font-weight: 600;
    margin-bottom: 0.5rem;
  }

  .location-info p {
    margin-bottom: 1.5rem;
    opacity: 0.9;
  }

  .map-actions {
    display: flex;
    gap: 1rem;
    justify-content: center;
    flex-wrap: wrap;
  }

  .map-btn {
    background: var(--primary-red);
    color: var(--white);
    border: none;
    padding: 0.7rem 1.2rem;
    border-radius: 6px;
    cursor: pointer;
    font-weight: 500;
    transition: background 0.3s ease;
  }

  .map-btn:hover {
    background: var(--dark-red);
  }

  .map-placeholder iframe {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
  }

  /* Mobile Styles */
  @media (max-width: 768px) {
    .contact-content {
      grid-template-columns: 1fr;
      gap: 2rem;
    }

    .form-row {
      grid-template-columns: 1fr;
    }

    .info-card,
    .form-card {
      padding: 2rem;
    }

    .map-section {
      padding: 2rem 1rem;
    }

    .map-actions {
      flex-direction: column;
      align-items: center;
    }

    .map-btn {
      width: 200px;
    }
  }
</style>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    const form = document.getElementById('contact-form');

    form?.addEventListener('submit', (e) => {
      e.preventDefault();

      const formData = new FormData(form);
      const data = Object.fromEntries(formData);

      // Simulate form submission
      const submitBtn = form.querySelector('button[type="submit"]');
      const originalText = submitBtn.textContent;

      submitBtn.textContent = 'Sending...';
      submitBtn.disabled = true;

      setTimeout(() => {
        alert('Thank you! Your message has been sent. We\'ll get back to you within 24 hours.');
        form.reset();
        submitBtn.textContent = originalText;
        submitBtn.disabled = false;
      }, 1500);
    });
  });

  function openDirections() {
    const address = encodeURIComponent('439B Main Street, Port Washington, NY 11050');
    window.open(`https://www.google.com/maps/dir/?api=1&destination=${address}`, '_blank');
  }

  function viewLargerMap() {
    const address = encodeURIComponent('439B Main Street, Port Washington, NY 11050');
    window.open(`https://www.google.com/maps/search/${address}`, '_blank');
  }
</script>